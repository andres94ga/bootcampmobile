{\rtf1\ansi\ansicpg1252\cocoartf2821
\cocoatextscaling0\cocoaplatform0{\fonttbl\f0\fswiss\fcharset0 Helvetica;}
{\colortbl;\red255\green255\blue255;}
{\*\expandedcolortbl;;}
\paperw11900\paperh16840\margl1440\margr1440\vieww11520\viewh8400\viewkind0
\pard\tx720\tx1440\tx2160\tx2880\tx3600\tx4320\tx5040\tx5760\tx6480\tx7200\tx7920\tx8640\pardirnatural\partightenfactor0

\f0\fs24 \cf0 import Foundation\
\
// Inicializar las pilas con contadores\
var pilas = [3, 5, 7]\
var turno = 1 // Jugador 1 empieza\
\
// Bucle principal del juego\
while true \{\
    // Mostrar estado actual de las pilas\
    print("Estado actual de las pilas:")\
    for (index, pila) in pilas.enumerated() \{\
        print("Pila \\(index + 1): \\(pila)")\
    \}\
    \
    // Verificar si todas las pilas est\'e1n vac\'edas\
    var juegoTerminado = true\
    for pila in pilas \{\
        if pila > 0 \{\
            juegoTerminado = false\
            break\
        \}\
    \}\
    \
    if juegoTerminado \{\
        print("Jugador \\(turno) ha tomado el \'faltimo contador y ha perdido.")\
        print("Jugador \\(turno == 1 ? 2 : 1) es el ganador!")\
        break\
    \}\
    \
    var pilaElegida: Int? = nil\
    var cantidadQuitar: Int? = nil\
    \
    // Validar elecci\'f3n de pila\
    repeat \{\
        print("Jugador \\(turno), elige una pila (1, 2 o 3):", terminator: " ")\
        pilaElegida = Int(readLine() ?? "")\
    \} while pilaElegida == nil || pilaElegida! < 1 || pilaElegida! > 3 || pilas[pilaElegida! - 1] == 0\
    \
    // Validar cantidad a quitar\
    repeat \{\
        print("Jugador \\(turno), elige cu\'e1ntos contadores quitar (m\'ednimo 1, m\'e1ximo \\(pilas[pilaElegida! - 1])):", terminator: " ")\
        cantidadQuitar = Int(readLine() ?? "")\
    \} while cantidadQuitar == nil || cantidadQuitar! < 1 || cantidadQuitar! > pilas[pilaElegida! - 1]\
    \
    // Actualizar la pila seleccionada\
    pilas[pilaElegida! - 1] -= cantidadQuitar!\
    \
    // Cambiar turno\
    turno = (turno == 1) ? 2 : 1\
\}}